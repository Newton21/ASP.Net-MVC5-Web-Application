@using UMSMVC5.Helpers
@model IEnumerable<UMSMVC5.Models.CourseAssign>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Course.CourseCode)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Teacher.TeacherName)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.Course.CourseCode)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Teacher.TeacherName)
        </td>
        <td>
            
            @Html.NoEncodeActionLink("<span class='glyphicon glyphicon-pencil'></span>", "Edit", "Edit", "CourseAssign", routeValues: new { id = item.CourseAssignId }, htmlAttributes: new { data_modal = "", @class = "btn btn-default" })
@Html.NoEncodeActionLink("<span class='glyphicon glyphicon-tasks'></span>", "Details", "Details", "CourseAssign", routeValues: new { id = item.CourseAssignId }, htmlAttributes: new { data_modal = "", @class = "btn btn-default" })
@Html.NoEncodeActionLink("<span class='glyphicon glyphicon-trash'></span>", "Delete", "Delete", "CourseAssign", routeValues: new { id = item.CourseAssignId }, htmlAttributes: new { data_modal = "", @class = "btn btn-danger" })

            @*@Html.ActionLink("Edit", "Edit", new { id=item.CourseAssignId }) |
            @Html.ActionLink("Details", "Details", new { id=item.CourseAssignId }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.CourseAssignId })*@
        </td>
    </tr>
}

</table>
