@using UMSMVC5.Helpers
@model IEnumerable<UMSMVC5.Models.Enrollment>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.RegistrationId)
        
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Course.CourseCode)
          
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Grade.GradeLetter)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.EnrollmentDate)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.RegistrationId)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Course.CourseCode)
          
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Grade.GradeLetter)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.EnrollmentDate)
        </td>
        <td>
            
            @*@Html.NoEncodeActionLink("<span class='glyphicon glyphicon-pencil'></span>", "Edit", "Edit", "Enrollment", routeValues: new { id = item.EnrollmentId }, htmlAttributes: new { data_modal = "", @class = "btn btn-default" })*@
            @Html.NoEncodeActionLink("<span class='glyphicon glyphicon-tasks'></span>", "Details", "Details", "Enrollment", routeValues: new { id = item.EnrollmentId }, htmlAttributes: new { data_modal = "", @class = "btn btn-default" })
            @Html.NoEncodeActionLink("<span class='glyphicon glyphicon-trash'></span>", "Delete", "Delete", "Enrollment", routeValues: new { id = item.EnrollmentId }, htmlAttributes: new { data_modal = "", @class = "btn btn-danger" })

        </td>
    </tr>
}

</table>
